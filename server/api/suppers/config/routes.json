{
  "routes": [
    {
      "method": "GET",
      "path": "/suppers",
      "handler": "suppers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/suppers/count",
      "handler": "suppers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/suppers/:id",
      "handler": "suppers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/suppers",
      "handler": "suppers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/suppers/:id",
      "handler": "suppers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/suppers/:id",
      "handler": "suppers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
